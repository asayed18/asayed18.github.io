import './ScrollHint.css'

export function ScrollHint() {
  return (
    <div className="scroll-hint">
      <p className="scroll-hint-line">Scroll to journey forward</p>
      <p className="scroll-hint-line scroll-hint-sub">
        Approach buildings to read about experiences
      </p>
    </div>
  )
}
